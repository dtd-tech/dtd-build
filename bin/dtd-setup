#!/bin/bash

# DTD-TOOLS Installer
# Will install requirements for the DTD build environment.

# Am I root?

# Add dtd bin directory 
# Check disk space

# Install dependencies
apt-get update -y
apt-get install -y git uck virtualbox

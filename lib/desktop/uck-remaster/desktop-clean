# DTD-Tools Desktop setup.
# To be run on a clean Ubuntu desktop install.

# Cleanup
apt-get -q -y purge libreoffice-core thunderbird libreoffice-common libreoffice-writer libreoffice-help-en-us libreoffice-calc hplip libsane libreoffice-draw libgutenprint2 empathy-common shotwell libgstreamer0.10 rhythmbox libreoffice-impress geoip-database unity-webapps-common

apt-get -q -y autoremove


# update apt, upgrade.
apt-get update
apt-get -q -y upgrade

# kickoff stack installer
